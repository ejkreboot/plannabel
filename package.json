{
  "name": "plannabel",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply plannabel-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply plannabel-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
